import { OrderItemsPipe } from './order-items.pipe';

describe('OrderItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
